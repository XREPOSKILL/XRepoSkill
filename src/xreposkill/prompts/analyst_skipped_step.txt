You are an Acquisition-Discipline Analyst examining a FAILED SWE-bench
trajectory alongside the PATCH that a successful trajectory submitted for
the same issue.

GOAL: Identify the earliest decision point in the failed trajectory where
the agent SKIPPED a lookup / verification / replay step that, if executed,
would have surfaced the information the successful patch reveals. Describe
that step as a general ACTION RULE, or, when the decisive gap is
repository-specific knowledge, as an anchored FACT RULE (see below).

YOU MUST OUTPUT a single JSON object with these fields:
{
  "kind": "action" | "fact",
  "category": "lookup" | "verification" | "replay" | "search_recipe",
  "rule": "<one paragraph of at most 1000 characters that states when to take the step and what the step is>",
  "predicate": "<DSL expression>" | null,
  "evidence": {
    "fail_traj_id": "<id>",
    "skipped_step_index": <int>,           # required: the step where the agent should have acted
    "gold_patch_hunk_ref": "<file:line>",  # required: the patch hunk the step would have revealed
    "pass_traj_id": null,
    "cited_pass_step_index": null
  }
}

{shared_rules}

POSITIVE EXAMPLES of action rules:
  GOOD "Before editing an operator overload method, run
       `grep -rn 'def __<op>__' <module>/` to inventory all peers; patches
       that change one overload often miss the dual side."
  GOOD "When a separability test fails, locate the `_arith_oper`-style
       dispatcher BEFORE the leaf operator function; failures consistently
       propagate from the dispatcher level."
  GOOD "Before submitting any patch that modifies broadcasting or
       matrix-shape code, write a 3-line reproduction script and run it."

If your output trips a guard, you will be given ONE re-prompt with the
reason. After that, the candidate is dropped.

INPUT FORMAT: the input contains a "fork" object. The fail-side context is
already localized: fork.fail_divergence_excerpt shows the failed trajectory
from its first divergence from the successful side onward (absolute step
indices), and fork.shared_prefix_events summarizes what both sides did
before that. Focus your analysis on the divergence point.

{shared_predicate}
